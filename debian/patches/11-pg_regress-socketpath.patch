diff -Nur -x '*.orig' -x '*~' postgresql-9.0-9.0~alpha4//src/test/regress/pg_regress.c postgresql-9.0-9.0~alpha4.new//src/test/regress/pg_regress.c
--- postgresql-9.0-9.0~alpha4//src/test/regress/pg_regress.c	2010-01-02 18:58:15.000000000 +0200
+++ postgresql-9.0-9.0~alpha4.new//src/test/regress/pg_regress.c	2010-03-27 22:59:11.000000000 +0200
@@ -899,7 +899,7 @@
 
 	/* And now we can build and execute the shell command */
 	snprintf(psql_cmd, sizeof(psql_cmd),
-			 SYSTEMQUOTE "\"%s%spsql\" -X -c \"%s\" \"%s\"" SYSTEMQUOTE,
+			 SYSTEMQUOTE "\"%s%spsql\" -h /tmp -X -c \"%s\" \"%s\"" SYSTEMQUOTE,
 			 psqldir ? psqldir : "",
 			 psqldir ? "/" : "",
 			 query_escaped,
@@ -2153,7 +2153,7 @@
 		 * Check if there is a postmaster running already.
 		 */
 		snprintf(buf2, sizeof(buf2),
-				 SYSTEMQUOTE "\"%s/psql\" -X postgres <%s 2>%s" SYSTEMQUOTE,
+				 SYSTEMQUOTE "\"%s/psql\" -h /tmp -X postgres <%s 2>%s" SYSTEMQUOTE,
 				 bindir, DEVNULL, DEVNULL);
 
 		for (i = 0; i < 16; i++)
@@ -2185,7 +2185,7 @@
 		 */
 		header(_("starting postmaster"));
 		snprintf(buf, sizeof(buf),
-				 SYSTEMQUOTE "\"%s/postgres\" -D \"%s/data\" -F%s -c \"listen_addresses=%s\" > \"%s/log/postmaster.log\" 2>&1" SYSTEMQUOTE,
+				 SYSTEMQUOTE "\"%s/postgres\" -D \"%s/data\" -k /tmp -F%s -c \"listen_addresses=%s\" > \"%s/log/postmaster.log\" 2>&1" SYSTEMQUOTE,
 				 bindir, temp_install,
 				 debug ? " -d 5" : "",
 				 hostname ? hostname : "",
diff -Nur -x '*.orig' -x '*~' postgresql-9.0-9.0~alpha4//src/test/regress/pg_regress_main.c postgresql-9.0-9.0~alpha4.new//src/test/regress/pg_regress_main.c
--- postgresql-9.0-9.0~alpha4//src/test/regress/pg_regress_main.c	2010-01-02 18:58:16.000000000 +0200
+++ postgresql-9.0-9.0~alpha4.new//src/test/regress/pg_regress_main.c	2010-03-27 23:00:57.000000000 +0200
@@ -59,7 +59,7 @@
 	add_stringlist_item(expectfiles, expectfile);
 
 	snprintf(psql_cmd, sizeof(psql_cmd),
-			 SYSTEMQUOTE "\"%s%spsql\" -X -a -q -d \"%s\" < \"%s\" > \"%s\" 2>&1" SYSTEMQUOTE,
+			 SYSTEMQUOTE "\"%s%spsql\" -h /tmp -X -a -q -d \"%s\" < \"%s\" > \"%s\" 2>&1" SYSTEMQUOTE,
 			 psqldir ? psqldir : "",
 			 psqldir ? "/" : "",
 			 dblist->str,
